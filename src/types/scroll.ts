export interface ScrollItem {
  id: string
  title: string
  summary: string
  source: string
  url: string
  topic: string
  publishedAt: string
}

export interface ScrollTopic {
  id: string
  label: string
}
